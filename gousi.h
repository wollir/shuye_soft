#ifndef GOUSI_H
#define GOUSI_H
/*

 1. 创建当前存在节点的结构体，成员： 编号，激活指令，接收数据，，液位数据数据更新时间。
    声明5个结构体指针数组，初始化 NUL。创建5个文件夹
 2. 如果点击了扫描终端，就调用call_terminal。
    来注册指针数组的标志。存在的节点在combox 中显示。
 3. 若点击激活，或者自动激活，调用分别调用存在节点的激活函数，如果，一段时间收到数据，就更新数据（表，txt），
 并将记录信息（数据，液位，时间）放进激活的txt文件,若端口掉线，就在表中显示掉线。

















*/
#endif // GOUSI_H
